COPY crosshair_config (
player_id,
color_index,
advanced_crosshair_settings,
custom_color,
fire_accuracy_fade,
follow_recoil,
show_outlines,
outline_thickness,
outline_opacity,
show_center_dot,
use_ads_settings,
center_dot,
center_dot_ads,
sniper_dot,
inner_pip,
outer_pip,
crosshair_config_version
) FROM STDIN (FORMAT BINARY);